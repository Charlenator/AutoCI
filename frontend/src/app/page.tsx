import ChatPanel from "../components/chat/ChatPanel";

// Sprint B2: live RAG chat tab. The Query Planner picks SQL templates / freeform
// SELECT / vector retrieval, the Citation Drawer renders sources, and the Query
// Transformation Card surfaces the routing decision before the answer.

export default function ChatPage() {
  return <ChatPanel />;
}
