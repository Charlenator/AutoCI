# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts


# Next.js build output
frontend/.next/
frontend/out/

# Dependency directories
node_modules/

# Local env files
.env*.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignore the Next.js build folder in the frontend directory
frontend/.next/

# Other standard ignores
node_modules/
.env*.local

# Python build artifacts
__pycache__/
*.pyc
*.pyo
*.pyd

# Python virtualenvs (Donna's box uses C:/autoci-venv; someone else might
# bind a venv inside the repo).
.venv/
venv/
backend/.venv/

# Claude Code local project state (settings, agent caches).
.claude/

# CV generator: test data is regeneratable; keep it local.
# Tooling (make_cvs.py + README.md) is committed; outputs and batches are not.
dev-tools/cv_generator/output/
dev-tools/cv_generator/*.json
!dev-tools/cv_generator/sample_batch.json

# Frontend design-pass bundle: regeneratable from dev-tools/export-frontend-for-design.ps1.
frontend-design-export/
